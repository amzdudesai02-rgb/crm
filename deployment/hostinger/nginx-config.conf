# Nginx configuration for api.leveragecrm.amzdudes.io
# Place this file at: /etc/nginx/sites-available/leveragecrm-api

server {
    listen 80;
    server_name api.leveragecrm.amzdudes.io;

    # Redirect HTTP to HTTPS (after SSL setup)
    # Uncomment after SSL is configured:
    # return 301 https://$server_name$request_uri;

    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Port $server_port;
        
        # WebSocket support (if needed)
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}

# HTTPS configuration (after SSL certificate is installed)
# Uncomment and update after running certbot:

# server {
#     listen 443 ssl http2;
#     server_name api.leveragecrm.amzdudes.io;
#
#     ssl_certificate /etc/letsencrypt/live/api.leveragecrm.amzdudes.io/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/api.leveragecrm.amzdudes.io/privkey.pem;
#
#     location / {
#         proxy_pass http://127.0.0.1:8000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_set_header X-Forwarded-Host $host;
#         proxy_set_header X-Forwarded-Port $server_port;
#         
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#     }
# }

